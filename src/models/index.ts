import { User } from '../components/users/user.model'
import { Role } from '../components/roles/roles.models'
import { Permission } from '../components/roles/permissions.models'
import { RolePermission } from '../components/roles/rolePermissions.models'

export const models = [User, Role, Permission, RolePermission]

export { User, Role, Permission, RolePermission }
